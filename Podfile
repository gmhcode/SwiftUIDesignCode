# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftUIDesignCode' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftUIDesignCode
	
pod 'Firebase/Analytics'
pod 'Firebase/Auth'
pod 'Firebase/Firestore'

  target 'SwiftUIDesignCodeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftUIDesignCodeUITests' do
    # Pods for testing
  end

end
